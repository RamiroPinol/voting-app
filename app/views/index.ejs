<!DOCTYPE html>
<html>

<% include blocks/head.ejs %>

<body>

  <% include blocks/navbar.ejs %>

  <div class="container">

    <div class="row">
      <% polls.forEach( poll => { %>
      <%  const pollName = poll.pollName %>
      <%  const userName = poll.userId %>
      <%  const options = poll.options %>
      <%  const id = poll._id.toString() %>
      <% include blocks/poll.ejs %>
      <%  }) %>
    </div>

    <div class="jumbotron text-center">
      <h1><span class="fa fa-pie-chart"></span> RP Voting App</h1>

      <p>Login or Register with:</p>

      <a href="/login" class="btn btn-default"><span class="fa fa-user"></span> Local Login</a>
      <a href="/signup" class="btn btn-default"><span class="fa fa-user"></span> Local Signup</a>
      <a href="/auth/facebook" class="btn btn-primary"><span class="fa fa-facebook"></span> Facebook</a>
      <a href="/auth/twitter" class="btn btn-primary"><span class="fa fa-twitter"></span> Twitter</a>
      <a href="/auth/google" class="btn btn-danger"><span class="fa fa-google-plus"></span> Google</a>
    </div>

</body>
<script src="/reload/reload.js"></script>
</html>
